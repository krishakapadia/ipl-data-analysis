This folder contains the small processed datasets used in the notebook.

Large files such as `IPL.csv` and `ipl_cleaned_final.csv` are not included in the repository due to GitHub's 100MB size limit.

To run the notebook, place the raw IPL dataset inside this folder before running any cells.
